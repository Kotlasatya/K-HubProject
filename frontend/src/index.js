import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Use createRoot to start rendering your app
ReactDOM.createRoot(document.getElementById('root')).render(<App />);
